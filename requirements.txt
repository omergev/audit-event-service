# requirements.txt

# --- app runtime ---
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic[email]==2.11.7
SQLAlchemy==2.0.42
alembic==1.16.4
redis==5.0.*


# Choose ONE of the Postgres stacks ↓
# (A) modern sync psycopg3
psycopg[binary]==3.2.3
# (B) async driver (use URL postgresql+asyncpg://...)
# asyncpg==0.30.0

# JSON Schema validation (strict format checks)
jsonschema[format-nongpl]==4.22.0

# Server-Sent Events helper (optional; handy for /stream)
sse-starlette==2.1.0

# --- tests ---
pytest==8.4.1
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.28.1

# --- utils (optional but helpful) ---
python-dotenv==1.0.1
